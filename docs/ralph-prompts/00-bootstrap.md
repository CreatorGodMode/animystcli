You are executing Phase 00 of the Animyst autonomous refactor.

Read `docs/ralph-tasks/00-bootstrap.md`, inspect the repository, perform only the listed scope, run the verification commands, and stop after creating the scoped commit.

Rules:
- do not revert unrelated changes
- request approval immediately if a required command is blocked
- record failures and next step in the phase log before stopping

