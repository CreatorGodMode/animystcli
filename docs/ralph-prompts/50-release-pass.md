You are executing Phase 50 of the Animyst next-step Ralph loop.

Read `docs/ralph-tasks/50-release-pass.md`, finish the release pass, verify the repo, and stop after the scoped commit or publication step.

Rules:
- keep docs aligned with the shipped product
- summarize remaining risks in the final phase log
- push and merge only when the configured workflow allows it
